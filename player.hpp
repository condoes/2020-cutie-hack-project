#ifndef _PLAYER_HPP
#define _PLAYER_HPP

#include <iostream>
#include <string>

using namespace std;

class Player {
    private:
        int wins;
        vector<card> playerDeck;
        
    public:
        Player();
};

#endif _PLAYER_HPP